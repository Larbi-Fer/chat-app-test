<nav class="nav">
    <input type="checkbox" class="check" id="check">
    <label for="check">
            <li><img src="/img/R3.png" alt="القائمة" class="checkbtn"></li>
        </label>
    <label for="" class="logo">online shope</label>
    <ul>
        <li><a href="/" <%= pageTitle === 'Home' ? 'class=active': '' %>>Home</a></li>
        <% if (isUser) { %>
            <li><a href="/cart" <%= pageTitle === 'Cart' ? 'class=active': '' %>>Cart</a></li>
            <li><a href="/orders" <%= pageTitle === 'Orders' ? 'class=active': '' %>>Orders</a></li>
        <% } else { %>
            <li><a href="/login" <%= pageTitle === 'Login' ? 'class=active': '' %>>Login</a></li>
            <li><a href="/singup" <%= pageTitle === 'Singup' ? 'class=active': '' %>>Singup</a></li>
        <% } %>
        <% if (isUser) { %>
            <li class="nav-li" style="color: #fff">
                <span title="Friend Requests" class="glyphicon glyphicon-user" onclick="toggleClass('.friendReqestsDropdown','active');">
                    <span>Friend Requests</span>
                </span>

                <div class="list-group-1 friendReqestsDropdown">
                    <div dir="rtl" style="display: flex;justify-content: end;">
                        <h2 class="list-1"> Friend Requests </h2>
                    </div>
                    <table style="padding: 0;" id="iami">
                        <% if ( friendRequest.length === 0 ) { %> 
                            no frienf requests
                        <% } %>
                        <% for( let request of friendRequest ) { %>
                            <tr>
                                <td><a href="/profile/<%= request.id %>"><%= request.name %></a></td>
                            </tr>
                        <% } %>
                    </table>
                </div>

            </li>
            <li><a href="/logout" style="color: rgb(255, 159, 167); background: #cf1800;">Logout</a></li>
        <% } %>
        <!--<li>
            <input type="search">
            <button>search</button>
        </li>-->
    </ul>
</nav>